package chapter02.Mar16.Mar19

object Test01 {

  def main(args: Array[String]): Unit = {

  /*  var arr = Array("hello zhangsan hello scala hello scala", "tom jack hello jack tom scala lucy")

    println(arr.flatMap(_.split(" ")).groupBy(x => x).mapValues(_.length)
      .toList.sortWith((x, y) => x._2 > y._2))

    println("第二次" + arr.flatMap(_.split(" ")).groupBy(x => x).mapValues(_.size).toArray.sorted.toBuffer)
*/
    val a: Byte = 10
    val b: Int = 20
    val c: Byte = b.toByte
    println(c)



  }
}

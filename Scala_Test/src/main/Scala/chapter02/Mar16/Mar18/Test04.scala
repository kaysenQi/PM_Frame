package chapter02.Mar16.Mar18

object Test04 {

  def main(args: Array[String]): Unit = {

/*    var arr:Array[Any] = Array(11, "String", "kaysen", 222.22)
    println(arr.toBuffer)

    //创建元组
//    val v = tuple3(11, "tom", 33.22)  这样创建不对
    var list: List[(Int, String)] = List((1001, "zs"), (1002, "ls"), (1003, "ww"), (1004, "tq"))*/

   /* println(list)
    for (l <- list) {
      println(l)
      println(l._1)
      println(l._2)
    }

    println("---------------   ------xing符分割线--   --------------------")
    //创建元组
    val t01: (Int, String, String, Double) = (1001, "Tom", "Jack", 22.33)

    println(t01._1)
    println(t01._2)
    println(t01._3)

    val t_list:List[(Int, String)] = List(
      (101, "Jack"), (12, "Tom"), (103, "lucy"), (14, "Alex"), (152, "WOO"),
      (6, "kaysen"), (17, "Mark")
    )

    println(t_list)

    val tt = t_list.filter(x => x._1 % 2 == 0)
    for (i <- tt) {
      println(i)
    }*/

  /*
  val tt_01 = t_list.filter(x => x._2.length > 3)
    println(tt_01)

    //排序的三种方式
    // sorted  sortBy  sortWith
    val sorted = t_list.sorted
    println(sorted)

    var ss = t_list.sortBy(x => x._1 % 2 == 0)
    println("sortBy的是:" + ss)

    val tt01 = t_list.sortWith((x, y) => x._1 < y._1)
    println("sortWith的是:" + tt01)
    */

    



  }


}
